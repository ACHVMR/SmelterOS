/**
 * ═══════════════════════════════════════════════════════════════════════════════
 * SmelterOS-ORACLE LLM Module
 * OpenRouter integration with GLM 4.7 + Gemini 3 Flash fallback
 * ═══════════════════════════════════════════════════════════════════════════════
 */

export {
  OpenRouterClient,
  getOpenRouterClient,
  OPENROUTER_MODELS,
  type OpenRouterMessage,
  type OpenRouterRequest,
  type OpenRouterResponse,
  type LLMConfig,
  type LLMResult,
  type OpenRouterModelKey,
} from './openrouter-client';
