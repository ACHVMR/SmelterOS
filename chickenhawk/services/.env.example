# ═══════════════════════════════════════════════════════════════════════
# AVVA NOON × SmelterOS Environment Configuration
# ═══════════════════════════════════════════════════════════════════════

# ═══ OpenRouter (Primary LLM Provider) ═══
# GLM4.7 as default model
OPENROUTER_API_KEY=sk-or-v1-your-key-here
DEFAULT_MODEL=glm4-flash
FALLBACK_MODEL=gemini-flash

# ═══ GCP / Vertex AI (Fallback) ═══
GCP_PROJECT_ID=your-project-id
GCP_LOCATION=us-central1
GOOGLE_API_KEY=your-google-api-key

# ═══ OpenAI (For embeddings) ═══
OPENAI_API_KEY=sk-your-openai-key

# ═══ Anthropic (Optional) ═══
ANTHROPIC_API_KEY=sk-ant-your-key

# ═══ Stripe (Billing Bridge) ═══
STRIPE_SECRET_KEY=sk_test_your-stripe-key
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# ═══ Resend (Email) ═══
RESEND_API_KEY=re_your-resend-key

# ═══ AVVA NOON Governance ═══
VIBE_THRESHOLD=0.85
AVVA_NOON_COMPRESSION_TARGET=0.90
AVVA_NOON_CHARTER_PATH=/app/logs/charter.log
AVVA_NOON_LEDGER_PATH=/app/logs/ledger.log
AVVA_NOON_REPORTS_PATH=/app/reports

# ═══ Firebase (Optional) ═══
FIREBASE_PROJECT_ID=your-firebase-project
FIREBASE_CONFIG={}

# ═══ Agent Zero Customization ═══
AGENT_NAME=AVVA-NOON
AUTO_MEMORY_COUNT=5
MAX_TOOL_RESPONSE_LENGTH=30000

# ═══ HITL (Human-in-the-Loop) ═══
HITL_ENABLED=true
HITL_STATIONARY=true
HITL_TIMEOUT_MINUTES=30
HITL_NOTIFY_EMAIL=your-email@example.com
HITL_WEBHOOK_URL=https://hooks.slack.com/your-webhook

# ═══ ngrok (Local Tunnel for HITL Webhooks) ═══
NGROK_AUTHTOKEN=your-ngrok-authtoken
NGROK_DOMAIN=your-subdomain.ngrok.io

# ═══ E2B (Sandboxed Agent Execution) ═══
E2B_API_KEY=your-e2b-api-key
E2B_TEMPLATE_ID=chickenhawk-sandbox

